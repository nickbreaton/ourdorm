{
  "scripts": {
    "start": "harp server",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "build": "harp compile . dist"
  },
  "devDependencies": {
    "harp": "^0.23.0",
    "rimraf": "^2.6.0"
  }
}
