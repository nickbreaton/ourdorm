import styled from 'styled-components';

export default styled.span`
  user-select: none;
  position: absolute;
  top: 0.5em;
  left: 0.5em;
  font-family: 'Arial';
  font-weight: bold;
  color: rgba(0,0,0,0.3);
`;
